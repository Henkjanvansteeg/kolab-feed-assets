# kolab-feed-assets

Hosting for Google Merchant Center image overrides used in Kolab Lisbon feed experiments (Icarus Agency).
Files under `lifestyle/` are lifestyle composites built from Kolab's own product photography; the product itself is unaltered.
Public because Google's image crawler needs a direct URL. No credentials, no customer data.
